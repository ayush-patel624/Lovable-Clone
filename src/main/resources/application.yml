spring:
  application:
    name: Lovable-Clone

  datasource:
    url: jdbc:postgresql://localhost:9000/lovableclone?options=-c%20TimeZone=Asia/Kolkata
    username: ayush624
    password: greninja624
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true